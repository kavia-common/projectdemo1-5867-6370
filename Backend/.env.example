# MongoDB connection string (required)
MONGODB_URI=mongodb://localhost:27017

# Optional overrides
MONGODB_DB_NAME=network_devices
MONGODB_COLLECTION_NAME=devices

# App port for local run (the preview system already uses 3001)
PORT=3001
